import { FancyButtonDirective } from './fancy-button.directive';

describe('FancyButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FancyButtonDirective();
    expect(directive).toBeTruthy();
  });
});
