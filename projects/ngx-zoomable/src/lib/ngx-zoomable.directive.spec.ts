import { NgxZoomableDirective } from './ngx-zoomable.directive';

describe('NgxZoomableDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxZoomableDirective();
    expect(directive).toBeTruthy();
  });
});
